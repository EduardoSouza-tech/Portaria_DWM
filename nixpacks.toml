[phases.setup]
nixPkgs = ["python310", "pip"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r backend/requirements.txt"]

[phases.build]
cmds = ["python backend/init_db.py"]

[start]
cmd = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
