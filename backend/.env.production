# Configuração para conectar ao PostgreSQL do Railway
# Use este arquivo quando quiser trabalhar com o banco de produção

# IMPORTANTE: Você precisa da URL PÚBLICA do Postgres
# Para obter:
# 1. Acesse Railway → Postgres → Connect
# 2. Copie a "Postgres Connection URL" (a pública, não a interna)
# 3. Cole abaixo substituindo esta linha

DATABASE_URL=postgresql://postgres:SUA_SENHA@regiao.railway.app:PORTA/railway

# Copie as outras configs do .env
SECRET_KEY=portaria-secret-key-super-seguro-2024
QR_SECRET_KEY=chave-para-assinar-qr-codes-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
REDIS_URL=redis://localhost:6379/0
STORAGE_PATH=./storage
MAX_FILE_SIZE=10485760
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
DEBUG=True
